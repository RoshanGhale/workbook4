"use strict";

let teamName = "New York Knicks";
let position = teamName.indexOf("z");
console.log(position);

if (position == -1) console.log("not found");

let teams = [
  "Golden State Warrior",
  "New York Knicks",
  "Denver Nuggets",
  "Indiana Pacer",
];

let positionInArray = teams.indexOf("Golden State Warrior");
console.log(positionInArray);
